# Scripts

Executable entry points reproduce the synthetic validation, CIFAR-10 pilots,
frozen multi-seed confirmation and subsequent pathway/topology diagnostics.
Run scripts from the repository root with the package installed editable or
with `PYTHONPATH=src`; each script has a module docstring describing its scope.
